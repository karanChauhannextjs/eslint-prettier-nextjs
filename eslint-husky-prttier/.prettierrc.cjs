module.exports = {
  printWidth: 100, // Line length
  tabWidth: 2, // Indentation
  useTabs: false, // Use spaces instead of tabs
  semi: true, // Always use semicolons
  singleQuote: true, // Use single quotes
  quoteProps: 'consistent', // Quotes around object properties only when needed
  trailingComma: 'all', // Trailing commas where possible
  bracketSpacing: true, // Spaces inside object literals
  bracketSameLine: false, // Put > on new line in JSX
  arrowParens: 'always', // Always include parentheses in arrow functions
  endOfLine: 'lf', // Unix style line endings
  proseWrap: 'preserve', // Markdown formatting
  embeddedLanguageFormatting: 'auto', // Format code inside template literals
};
